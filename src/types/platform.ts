export type Platform = 'windows' | 'ios' | 'android' | 'web'

export const PLATFORM_LABELS: Record<Platform, string> = {
  windows: 'Windows',
  ios: 'iPhone',
  android: 'Android',
  web: 'Веб-сервис',
}
